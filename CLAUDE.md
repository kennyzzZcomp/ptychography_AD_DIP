## Execution Rules
- My Main code is "ProPtyNet_torch.py". This is the main simulation code. It has my main idea of research.
- NEVER run long simulation scripts, full training loops, or intensive ptychography reconstructions automatically after editing code.
- Report changes immediately to the user with the exact command for them to run manually.