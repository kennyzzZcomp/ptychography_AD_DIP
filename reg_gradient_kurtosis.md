# 幅值梯度正则：三个候选方案

针对 `simulations/INNM_Ptycho.ipynb`。作用对象是**物体幅值** `|O|`（模型里的 `out1`，权重由 `tv1` 控制）。
相位那一路 `tv2` 先设 0，等幅值这边跑通再说。

---

## 0. 为什么从 K(I) 换到 K(∇O)

### 已经证伪的做法：对强度直接求峰度

`ic.kurtosis_loss` 算的是 `K(|O|²)`，即**强度直方图**的峰度。它隐含的假设是"图像本身是稀疏的"——
暗背景上的孤立亮点。这对细胞、USAF 靶成立，对 cameraman 这种自然图像**根本不成立**。

实测失败模式（`KURT_BETA=1e-6`，14 stage）：

- 数据项镇得住有约束的区域，所以中心 96×96 的 PSNR 仍有 30+
- 但峰度在**弱约束/无约束区域**打洞：单个像素被推到极端，全图 `imshow` 色标被撑爆，看上去全黑
- `lr` 越大越差、训练再久也不改善 —— 这是"目标函数最优解在错误位置"的特征，
  优化越充分离错误解越近。不是调参问题。

### 换成梯度域的依据

自然图像的**梯度**分布是重尾的（Field 1987 以来的经典结论）：绝大部分区域平坦、
梯度≈0，少数边缘处梯度很大。模糊会把这个分布往高斯推——尾巴变轻、峰度下降。

所以"最大化梯度峰度" ≈ "分片平坦 + 锐利边缘"，这正是盲去卷积里的标准自然图像先验。
方向上也更稳固：模糊必然减少锐利边缘、降低梯度峰度，不像强度峰度那样会被相干条纹反转符号。

---

## 记号

对幅值图 `u = |O|`（在 `REG_CROP` 区域内）：

```
dx[i,j] = u[i+1,j] - u[i,j]
dy[i,j] = u[i,j+1] - u[i,j]
g       = concat(flatten(dx), flatten(dy))        # 长度 n ≈ 2N²
```

**`dx` 和 `dy` 要拼成一个向量再做统计**，不要分别算完再相加——后者算的是两个边缘方向各自的
分布，前者才是"梯度分布"。

---

## 方案 1：`-K(∇O)`

### 定义

```
ḡ  = mean(g)
m2 = mean((g - ḡ)²)
m4 = mean((g - ḡ)⁴)
K  = m4 / (m2² + ε)
L  = -β · K
```

### 性质

| | |
|---|---|
| 取值范围 | `K ∈ [1, n]`，下界来自 `m4 ≥ m2²` |
| 有无下界 | **无**。`L` 可以跌到 `-β·n ≈ -β·32000` |
| 尺度不变 | 是。`u → c·u` 不改变 K，所以对全局幅值缩放免疫 |
| 退化最优解 | 梯度处处为零、仅一处非零 ⇒ **一张常数图 + 单个阶跃** |

### 预期失败模式

不再是"打洞"，而是**卡通化**：图像被推成大片纯色 + 极少数硬边。
比 delta 函数温和，但仍然是退化解，同一类病。

### 超参

- `β`（在 `raw_kurtosis_loss` 里）—— 唯一超参
- 数据项量级约 1e-2 ~ 1e-4（随收敛变化），而 K 是 O(10) 量级的**标量**，
  所以 `β` 大致要落在 `1e-4 ~ 1e-6`。先用热身后的实测值定，别拍脑袋

---

## 方案 2：`‖∇O‖₁ / ‖∇O‖₂`（归一化稀疏度）

### 定义

```
L = β · ‖g‖₁ / (‖g‖₂ + ε)
```

**这一项是最小化**（不是最大化），符号与方案 1 相反但作用方向相同——都在推稀疏梯度。

### 出处

Krishnan et al. 2011, *Blind Deconvolution Using a Normalized Sparsity Measure*。
它解决的正是这里的问题：纯 `‖∇O‖₁` 会偏向模糊解（模糊降低总梯度，ℓ1 反而更小），
所以要除以 ℓ2 归一化。峰度 `m4/m2²` 本质上是同一类归一化统计量。

### 性质

| | |
|---|---|
| 取值范围 | `[1, √n]`，由 Cauchy–Schwarz 给出；`√n ≈ 180`（N=128 时） |
| 有无下界 | **有**，下界 1 |
| 尺度不变 | 是 |
| 退化最优解 | 仍然是"仅一处非零梯度" |

### 与方案 1 的实际差别

**有界 ≠ 无退化解**——两者的退化最优解是同一个。差别在于**优化器能从作弊里赚多少**：

- 方案 1：`-βK` 可以一路跌到 `-β·n`，跌幅无上限，最终必然压过数据项
- 方案 2：`β·(ℓ1/ℓ2)` 只能从 `β√n` 降到 `β`，总降幅封顶在 `β(√n - 1) ≈ 180β`

所以方案 2 不会出现"正则项越滚越大直到吃掉一切"，这是它更实用的地方。

### 超参

- `β` —— 唯一超参。同样按"占数据项百分之几"来定

---

## 方案 3：`(K(∇O) − K_target)²`（靶向峰度）

### 定义

```
L = β · (K(g) - K_target)²
```

### 性质

| | |
|---|---|
| 有无下界 | **有**，下界 0 |
| 退化最优解 | **无**。最优是"梯度峰度等于目标值"，不是任何极端图像 |
| 代价 | 多一个超参 `K_target`，而且它依赖样品 |

三个方案里只有这个把退化解也一并消掉了。

### `K_target` 怎么定 —— 这一步别作弊

**用一次纯 MSE 重建的结果去量，不要用真值。**用真值量属于 inverse crime，
消融结论会虚高，跟之前用 Zernike 合成真值探针是同一个错误。

```
1. tv1=0 跑够 stage，拿到纯 MSE 重建 O_mse
2. K_target = kurtosis(∇|O_mse|)
3. 带 (K - K_target)² 正则重跑
```

真实实验里可以从同类样品的先验、或分辨率靶的重建里取。

---

## 实现要点（三个方案共通）

### 初始化时会 0/0

物体初值是常数 1 ⇒ 梯度处处为零 ⇒ `m2 = 0`、`‖g‖₂ = 0`。

- 方案 1、3：`K = 0/0`
- 方案 2：`ℓ1/ℓ2 = 0/0`

分母的 `ε` 能防 nan，但那时 K 恒为 0、梯度也接近 0，正则**完全不起作用**。
所以**必须先无正则热身几个 stage**，等物体有了结构再接正则。这也是量 `β` 和 `K_target` 的前提。

### 与 TV/TGV 不同：这些是标量，不是逐像素求和

`make_tv_loss` / `make_tgv_loss` 是对每个像素的惩罚求和（或求平均），所以有 `reduce='sum'/'mean'`
的量级问题。而峰度、ℓ1/ℓ2 都是**已经归一化的比值**，本身就是一个标量，
`reduce` 参数对它们没有意义。

代价是它们的量级完全不同（K 是 O(10)，TGV-sum 是 O(1e3)），
**不能把 TGV 调好的 `tv1` 直接搬过来**，必须重新标定。

### 权重怎么定

数据项（Keras `loss='mse'`，元素均值）在热身后约 1e-2，随收敛还会掉一两个数量级。
按"正则占数据项 10%"反推：

```python
# 热身后
pred0 = pty.OBJ.predict([input0, input1, input2], batch_size=1, verbose=0)[0]
y0 = np.sqrt(np.maximum(input2, 0)).astype(np.float32)
data = float(np.mean((pred0 - y0)**2))
reg  = float(该正则(None, tf.constant(out1)))     # 不带 β 的原始值
beta = 0.1 * data / abs(reg)
```

注意 L1 型正则的梯度不随收敛衰减，而数据项会——所以同一个 `β` 在训练后期的
**相对强度会自动变大**。表现是先出结构、后期越训越糊。必要时让 `β` 跟数据项一起衰减。

---

## 怎么判断成功/失败

**不要只看 PSNR。**上次的教训：病态可以完全藏在指标够不到的地方——
`EVAL_CROP=16` 只算中心 96×96，而 `REG_CROP=0` 时正则作用于全图，
尖峰堆在边缘带里，PSNR 照样 30+。

每个 stage 至少记这几个（用 `train(..., on_stage_end=...)` 回调）：

| 量 | 看什么 |
|---|---|
| `Rmax = max(I)/mean(I)`，**全图** | 尖峰的直接证据。正常重建应在个位数 |
| `K(∇\|O\|)` 本身 | 和 `K_target`、和真值的梯度峰度对比 |
| 中心 96×96 的 PSNR / SSIM | 有约束区域的质量 |
| `max|O|` 全图 vs 中心 96×96 | 两者差很多就说明尖峰在 ROI 外 |

显示时记得 `vmax=np.percentile(amp, 99)`，否则单个尖峰会把色标撑爆、整幅变黑。

---

## 建议顺序

1. **方案 1** —— 先看梯度域是不是真的比强度域好。就算它照样退化，
   失败模式（卡通化 vs 打洞）本身就是有信息量的结果
2. **方案 2** —— 有界，看"限制作弊收益"能不能救回来
3. **方案 3** —— 三者中唯一没有退化解的，作为上界参考

对照组始终是 `tv1=0` 的纯 MSE 基线（当前配置 12 stage：SSIM 0.892 / PSNR 23.70 dB）。
